# Trans MCP 环境变量配置
#
# 说明：stdio 模式（trans-mcp）和 HTTP 模式（trans-mcp-http）读的变量不一样，
# 下面分开列。部署 HTTP 服务只需要 MCP_* 这几个。

# ---------- stdio 模式 ----------

# Belindoc API Key
# 仅 stdio 模式需要（服务跑在你自己机器上，用你自己的 key）。
# HTTP 模式不读这个变量：每个客户端自己带 Authorization: Bearer <key>，
# 服务器不存任何密钥——所以别把真实 key 写进服务器的 .env。
# 格式: ft_ + 40位随机串，共43字符
# 获取方式: 登录 https://belindoc.com → 开放平台 → API Key 管理
BELINDOC_API_KEY=ft_your_api_key_here

# ---------- HTTP 模式 ----------

# 监听地址 (可选，默认 0.0.0.0)
# MCP_HOST=0.0.0.0

# 监听端口 (可选，默认 8080)
# MCP_PORT=8080

# MCP 服务端点路径 (可选，默认 /mcp)
# 同域名下落地页已占用 /mcp 时，把服务挪到别的路径。
# 客户端 URL 要同步改成 http://<host>:<port><MCP_PATH>
# MCP_PATH=/api/mcp

# 放开「读写本机文件」的工具 (可选，默认关闭)
# upload_file / get_upload_status / download_video_result / get_download_status
# 由服务端直接读写路径，只在服务端和用户是同一台机器时才有意义——否则「下载到
# 本地」落的是服务器的盘，还等于给持有 API Key 的人开了任意路径的写入面。
# 服务端靠来源地址判断不出来（隧道、反代进来的都是 loopback），所以由你显式声明。
# 开之前请确认 MCP_HOST 绑的是 127.0.0.1 而不是 0.0.0.0，否则同网段的人也能用。
# MCP_LOCAL_FS=1

# ---------- 上游地址 ----------

# 注意：上游 API 地址目前是写死的常量，不走环境变量，
# 要改只能改 src/trans_mcp/client.py:11 的 API_BASE_URL。
